node_modules/
www/
*.apk
*.aab
android/.idea/

# 簽章金鑰絕對不要進版控
*.jks
*.keystore
keystore.properties

# android/ 由 android/.gitignore 處理（build/、.gradle/、local.properties 等產物已排除），
# 原生程式碼 MainActivity.java 需納入版控，故此處不整個忽略。
